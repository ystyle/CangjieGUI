[package]
cjc-version = "1.0.5"
name = "game"
version = "0.1.0"
output-type = "executable"
description = "Thunder fighter example built on the Cangjie sdl module."

[dependencies]
sdl = { path = "../.." }
