[package]
cjc-version = "1.0.5"
name = "cui_dashed"
version = "0.1.0"
output-type = "executable"
description = "CUI dashed rounded borders (dashedBorder) — dropzone + adjustable preview."

[dependencies]
cui = { path = "../.." }
