[package]
cjc-version = "1.0.5"
name = "cui_links"
version = "0.1.0"
output-type = "executable"
description = "CUI RichText clickable link spans (onTap) — an activity feed with inline links."

[dependencies]
cui = { path = "../.." }
