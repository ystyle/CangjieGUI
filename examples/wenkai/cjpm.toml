[package]
cjc-version = "1.0.5"
name = "cui_wenkai"
version = "0.1.0"
output-type = "executable"
description = "CUI CJK font-file loading — a LXGW WenKai type specimen with weight switching and mono variant."

[dependencies]
cui = { path = "../.." }
