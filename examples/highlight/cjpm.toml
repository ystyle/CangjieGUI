[package]
cjc-version = "1.0.5"
name = "cui_highlight"
version = "0.1.0"
output-type = "executable"
description = "CUI RichSpan.highlight — live search-term highlighting (<mark>) over a knowledge-base reader."

[dependencies]
cui = { path = "../.." }
