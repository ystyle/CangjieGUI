[package]
cjc-version = "1.0.5"
name = "cui_cards"
version = "0.1.0"
output-type = "executable"
description = "CUI style modifiers (shadow elevation, corner radius, border) card playground."

[dependencies]
cui = { path = "../.." }
