[package]
cjc-version = "1.0.5"
name = "cui_shadows"
version = "0.1.0"
output-type = "executable"
description = "CUI layered shadows — Material key+ambient dual layer vs single layer, side by side."

[dependencies]
cui = { path = "../.." }
