[package]
cjc-version = "1.0.5"
name = "cui_skeleton"
version = "0.1.0"
output-type = "executable"
description = "CUI Pulse — a breathing skeleton loading screen that swaps to real content."

[dependencies]
cui = { path = "../.." }
